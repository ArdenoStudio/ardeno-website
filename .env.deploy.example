# Copy this file to .env.deploy.local on your machine.
# .env.deploy.local is ignored by Git. Do not commit real tokens.

VERCEL_TOKEN=

# Optional. If both are set, the deploy script can skip interactive project linking.
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
